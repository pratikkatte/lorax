# Production environment configuration for Lorax website
#
# This file is used during the production build (yarn build)
# to configure the frontend to connect to the production backend.
#
# Update VITE_API_BASE to match your deployed backend URL.
# The URL should be the full HTTPS URL without a trailing slash.

VITE_API_BASE=https://api.lorax.example.com
