"""CSV support for Lorax backend.

This package is intentionally minimal: it provides a config builder that matches
the existing frontend contract and a layout builder that can return an empty but
valid PyArrow IPC buffer for early CSV support.
"""

